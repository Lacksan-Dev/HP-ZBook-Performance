# Safety boundary

The controller refuses providers that omit declarations preserving Windows security, Windows Update, recovery, enterprise management, device-critical drivers, Omnissa, Windows App, Remote Desktop, or Tailscale. The initial profile is read-only. Transaction reports exclude credentials, registry payloads, usernames, user paths, and application content.
