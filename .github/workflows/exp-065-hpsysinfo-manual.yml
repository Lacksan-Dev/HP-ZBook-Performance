name: EXP-065 HP System Info HSA Manual

on:
  pull_request:
    paths:
      - 'controller/providers/HPSysInfoCapManual.ps1'
      - 'controller/tests/HPSysInfoCapManual*.Tests.ps1'
      - '.github/workflows/exp-065-hpsysinfo-manual.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  pester:
    runs-on: windows-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - name: Install Pester
        shell: pwsh
        run: |
          Set-PSRepository PSGallery -InstallationPolicy Trusted
          Install-Module Pester -MinimumVersion 5.5.0 -Scope CurrentUser -Force
      - name: Validate EXP-065 provider
        shell: pwsh
        run: |
          $config = New-PesterConfiguration
          $config.Run.Path = @(
            'controller/tests/HPSysInfoCapManual.Tests.ps1',
            'controller/tests/HPSysInfoCapManual.Integration.Tests.ps1'
          )
          $config.Run.Exit = $true
          $config.Output.Verbosity = 'Detailed'
          Invoke-Pester -Configuration $config