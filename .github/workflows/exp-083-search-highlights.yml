name: EXP-083 Windows Search highlights

on:
  pull_request:
    paths:
      - 'controller/providers/WindowsSearchDisableHighlights.ps1'
      - 'controller/tests/WindowsSearchDisableHighlights*.Tests.ps1'
      - '.github/workflows/exp-083-search-highlights.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  pester:
    runs-on: windows-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - name: Install Pester
        shell: pwsh
        run: |
          Set-PSRepository PSGallery -InstallationPolicy Trusted
          Install-Module Pester -MinimumVersion 5.5.0 -Scope CurrentUser -Force
      - name: Validate EXP-083 provider
        shell: pwsh
        run: |
          $config = New-PesterConfiguration
          $config.Run.Path = @(
            'controller/tests/WindowsSearchDisableHighlights.Tests.ps1',
            'controller/tests/WindowsSearchDisableHighlights.Integration.Tests.ps1'
          )
          $config.Run.Exit = $true
          $config.Output.Verbosity = 'Detailed'
          Invoke-Pester -Configuration $config
